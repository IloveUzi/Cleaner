Install Python from the website (NOT WINDOWS STORE)

Open cmd.exe and run those commands

pip install requests
pip install colorama

Files:

tokens.txt (Insert your tokens)

Params:

Token Type: (Combo = mail/pass/token | Token = token only)
Proxy: (username:password@ip:port OR ip:port | If you don't want to use proxy, leave empty)